# Conditional Logic

name = ask "What is your name?"
show "Hi " + name

score = 10

if score >= 90
    show "Grade: A"
else
    if score >= 80
        show "Grade: B"
    else
        show "Grade: C"

show "Done"
